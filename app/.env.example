# PostgreSQL — pgvector container (see docker-compose.yml)
DATABASE_URL=postgres://postgres:password@localhost:5432/vectordb

# JWT signing secret — generate with: openssl rand -hex 32
JWT_SECRET=replace-with-a-random-64-char-hex-string

# Python agent service URL (see agent/)
AGENT_SERVICE_URL=http://localhost:8000
